# Crux Project Bootstrap

Before interpreting project state, routing work, or loading agents, read:

  `{framework-home}/skills/crux-coordinator/SKILL.md`

Default `{framework-home}` is `$HOME/.crux` unless `CRUX_HOME` is set.

Treat `{framework-home}/` as reusable framework definitions and `{project}/.crux/` as project knowledge and live state. Do not write project knowledge into `{framework-home}/`.

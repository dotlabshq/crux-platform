# Crux Arc

This is the Crux framework root. Before doing anything in a project, read:

  `{framework-home}/skills/crux-arc/SKILL.md`

Default `{framework-home}` is `$HOME/.crux` unless `CRUX_HOME` is set.

The arc skill explains boot, routing, workspace initialisation, and how to load agents and skills on demand.

# Crux Coordinator

This is the Crux framework root. Before doing anything in a project, read:

  `{framework-home}/skills/crux-coordinator/SKILL.md`

Default `{framework-home}` is `$HOME/.crux` unless `CRUX_HOME` is set.

The coordinator skill explains boot, routing, workspace initialisation, and how to load agents and skills on demand.
